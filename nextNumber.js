const nextNumber = (num) => {

    //let str = parseInt(num.toString().split('').sort().reverse().join(''))
    let found = false
    while(!found) {
        
        let temp = parseInt(num.toString().split(''))
        console.log(temp)
        

        console.log(str)
    }
}

nextNumber(19)

nextNumber(3542)

nextNumber(5432)

nextNumber(58943)
